# Shared test configuration
# Activated by @ActiveProfiles("test") — avoid duplicating these in @SpringBootTest properties
spring:
  datasource:
    url: jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1;MODE=PostgreSQL
  flyway:
    enabled: false
  jpa:
    hibernate:
      ddl-auto: create-drop

github:
  webhook:
    secret: ""

novu:
  api:
    key: not-set
